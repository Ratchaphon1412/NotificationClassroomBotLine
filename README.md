# LINE Notification Classroom Bot

---

## fist step

- create python environments
  for MacOS:

```shell
    python3 -m venv env
    source env/bin/activate
```

for Windows:

```shell
    python3 -m venv env
    env\Scripts\activate.bat
```
